name: Email Step Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f2001045@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Final step
        run: echo "Done"
